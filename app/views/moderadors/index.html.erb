<h1>Listing moderadors</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Identidade</th>
    <th>Cpf</th>
    <th>Endereco</th>
    <th>Telefone</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @moderadors.each do |moderador| %>
  <tr>
    <td><%= moderador.nome %></td>
    <td><%= moderador.identidade %></td>
    <td><%= moderador.cpf %></td>
    <td><%= moderador.endereco %></td>
    <td><%= moderador.telefone %></td>
    <td><%= link_to 'Show', moderador %></td>
    <td><%= link_to 'Edit', edit_moderador_path(moderador) %></td>
    <td><%= link_to 'Excluir', moderador, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Moderador', new_moderador_path %>
